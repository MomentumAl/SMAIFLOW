// This component is no longer in use and has been replaced by InteractiveTour.tsx
// It can be safely deleted, but is kept here to avoid breaking imports if referenced elsewhere unexpectedly.
import React from 'react';

const WelcomeModal: React.FC = () => {
  return null;
};

export default WelcomeModal;
