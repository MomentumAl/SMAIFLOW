import React from 'react';

const Chatbot: React.FC = () => {
  // Este componente ya no se utiliza, ya que el chatbot personalizado ha sido
  // reemplazado por el servicio de Chatbase, que se integra mediante un script en index.html.
  // Devolver null asegura que no se renderice nada.
  return null;
};

export default Chatbot;
